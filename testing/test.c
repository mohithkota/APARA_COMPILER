//The minimal skeleton (put your answer in results[] so it gets auto-checked):
long long results[10];
int main() {
    // ... your computation ...
    for (int i=0;i<10;i++)
    {
	    results[i] = i ;
    }
    return 1;
}
