// ── APARA startup (APARA C Compiler v6: 28-reg + spilling) ──────────────────
// Fixed: $r0=ZERO $r28=GBASE=0x400  Reserved: $r26=FP $r27=SP
// Pool:  $r1-$r25 $r29-$r31  (28 registers, all recycled dynamically)
apara_start:
||
    $set $r28 0 1024
    $set $r27 0 65528
;
||
    + $r26 ($i64) $r0 $r27
    $call main
;
||
    $halt
;
main:
||
    $st ($i64) [$r27 + 0] $r26
    $set $r1 0 824
    + $r2 ($i64) $r0 0
;
||
    + $r26 ($i64) $r0 $r27
    - $r27 ($i64) $r27 $r1
;
||
    + $r1 ($i64) $r26 -8
;
||
    $st ($i32) [$r1 + 0] $r2
;
||
    + $r2 ($i64) $r26 -8
;
||
    $ld ($i32) $r3 [$r2 + 0]
;
wc_1:
||
    + $r4 ($i64) $r0 $r3
    + $r5 ($i64) $r0 64
;
||
    - $r6 ($i64) $r5 $r4
;
||
    ? ($i64) $r6 > $goto wb_2
;
||
    ? ($i64) $r0 == $goto we_3
;
wb_2:
||
    + $r5 ($i64) $r26 -16
    + $r6 ($i64) $r0 0
;
||
    $st ($i32) [$r5 + 0] $r6
;
||
    + $r6 ($i64) $r26 -16
;
||
    $ld ($i32) $r7 [$r6 + 0]
;
wc_4:
||
    + $r8 ($i64) $r0 $r7
    + $r9 ($i64) $r0 64
;
||
    - $r10 ($i64) $r9 $r8
;
||
    ? ($i64) $r10 > $goto wb_5
;
||
    ? ($i64) $r0 == $goto we_6
;
wb_5:
||
    + $r9 ($i64) $r0 $r3
    + $r11 ($i64) $r0 $r7
    + $r15 ($i64) $r0 256
    + $r19 ($i64) $r0 256
;
||
    * $r10 ($i64) $r9 64
;
||
    + $r12 ($i64) $r10 $r11
;
||
    + $r13 ($i64) $r12 1
;
||
    / $r16 ($i64) $r13 $r15
;
||
    * $r16 ($i64) $r16 $r15
    + $r15 ($i64) $r12 0
;
||
    - $r14 ($i64) $r13 $r16
    + $r15 ($i64) $r28 $r15
;
||
    $st ($i8) [$r15 + 0] $r14
;
||
    * $r15 ($i64) $r11 64
;
||
    + $r16 ($i64) $r15 $r9
;
||
    + $r17 ($i64) $r16 1
;
||
    / $r20 ($i64) $r17 $r19
;
||
    * $r20 ($i64) $r20 $r19
    $set $r19 0 4096
;
||
    - $r18 ($i64) $r17 $r20
    + $r19 ($i64) $r19 $r12
    + $r20 ($i64) $r11 1
;
||
    + $r19 ($i64) $r28 $r19
    + $r7 ($i64) $r0 $r20
;
||
    $st ($i8) [$r19 + 0] $r18
;
||
    + $r19 ($i64) $r0 $r11
    ? ($i64) $r0 == $goto wc_4
;
we_6:
||
    + $r21 ($i64) $r26 -16
    + $r22 ($i64) $r0 $r3
;
||
    $st ($i32) [$r21 + 0] $r7
    + $r23 ($i64) $r0 $r22
    + $r24 ($i64) $r22 1
;
||
    + $r3 ($i64) $r0 $r24
    ? ($i64) $r0 == $goto wc_1
;
we_3:
||
    + $r25 ($i64) $r26 -8
    + $r29 ($i64) $r26 -8
    + $r30 ($i64) $r0 0
;
||
    $st ($i32) [$r25 + 0] $r3
    $st ($i32) [$r29 + 0] $r30
;
wc_7:
||
    + $r30 ($i64) $r26 -8
    + $r1 ($i64) $r0 64
;
||
    $ld ($i32) $r31 [$r30 + 0]
;
||
    - $r2 ($i64) $r1 $r31
;
||
    ? ($i64) $r2 > $goto wb_8
;
||
    ? ($i64) $r0 == $goto we_9
;
wb_8:
||
    + $r1 ($i64) $r26 -16
    + $r2 ($i64) $r0 0
;
||
    $st ($i32) [$r1 + 0] $r2
;
wc_10:
||
    + $r2 ($i64) $r26 -16
    + $r5 ($i64) $r0 64
;
||
    $ld ($i32) $r4 [$r2 + 0]
;
||
    - $r6 ($i64) $r5 $r4
;
||
    ? ($i64) $r6 > $goto wb_11
;
||
    ? ($i64) $r0 == $goto we_12
;
wb_11:
||
    + $r5 ($i64) $r26 -8
    + $r13 ($i64) $r0 0
    $set $r16 0 4096
    + $r20 ($i64) $r0 0
    $set $r21 0 4096
    + $r31 ($i64) $r0 0
;
||
    $ld ($i32) $r6 [$r5 + 0]
;
||
    * $r8 ($i64) $r6 64
    $set $r6 0 4096
;
||
    + $r13 ($i64) $r13 $r8
    + $r29 ($i64) $r8 32
;
||
    + $r10 ($i64) $r28 $r13
    + $r13 ($i64) $r26 -16
    + $r31 ($i64) $r31 $r29
;
||
    $ld ($i32) $r14 [$r13 + 0]
    $ld ($u128) $r2 [$r10 + 0]
    + $r30 ($i64) $r28 $r31
;
||
    * $r9 ($i64) $r14 64
    + $r17 ($i64) $r0 $r3
    + $r29 ($i64) $r14 1
;
||
    + $r16 ($i64) $r16 $r9
    + $r31 ($i64) $r9 32
;
||
    + $r15 ($i64) $r28 $r16
    + $r16 ($i64) $r0 $r2
    + $r6 ($i64) $r6 $r31
    + $r31 ($i64) $r26 -16
;
||
    $ld ($u128) $r2 [$r15 + 0]
    + $r1 ($i64) $r28 $r6
;
||
    + $r12 ($i64) $r0 $r2
    + $r18 ($i64) $r0 $r3
    + $r3 ($i64) $r8 16
;
||
    $dot $r2 ($vu8) $r16 $r12
    + $r20 ($i64) $r20 $r3
    + $r16 ($i64) $r8 48
    + $r12 ($i64) $r0 0
;
||
    $dot $accumulate $r2 ($vu8) $r17 $r18
    + $r11 ($i64) $r28 $r20
    + $r20 ($i64) $r9 16
    + $r12 ($i64) $r12 $r16
;
||
    + $r21 ($i64) $r21 $r20
    $ld ($u128) $r4 [$r11 + 0]
    + $r17 ($i64) $r28 $r12
    + $r12 ($i64) $r9 48
    $set $r20 0 4096
;
||
    + $r7 ($i64) $r28 $r21
    + $r21 ($i64) $r0 $r4
    + $r22 ($i64) $r0 $r5
    + $r20 ($i64) $r20 $r12
;
||
    $ld ($u128) $r4 [$r7 + 0]
    + $r18 ($i64) $r28 $r20
;
||
    + $r24 ($i64) $r0 $r4
    + $r25 ($i64) $r0 $r5
;
||
    $dot $r4 ($vu8) $r21 $r24
;
||
    $dot $accumulate $r4 ($vu8) $r22 $r25
    + $r22 ($i64) $r8 $r14
    $set $r25 0 8192
;
||
    + $r5 ($i64) $r2 $r4
    $ld ($u128) $r2 [$r30 + 0]
    * $r24 ($i64) $r22 8
;
||
    + $r6 ($i64) $r0 $r2
    + $r13 ($i64) $r0 $r3
    $ld ($u128) $r2 [$r1 + 0]
    + $r25 ($i64) $r25 $r24
;
||
    + $r10 ($i64) $r0 $r2
    + $r15 ($i64) $r0 $r3
    + $r25 ($i64) $r28 $r25
;
||
    $dot $r2 ($vu8) $r6 $r10
;
||
    $dot $accumulate $r2 ($vu8) $r13 $r15
;
||
    + $r3 ($i64) $r5 $r2
    $ld ($u128) $r4 [$r17 + 0]
;
||
    + $r20 ($i64) $r0 $r4
    + $r11 ($i64) $r0 $r5
    $ld ($u128) $r4 [$r18 + 0]
;
||
    + $r7 ($i64) $r0 $r4
    + $r21 ($i64) $r0 $r5
;
||
    $dot $r4 ($vu8) $r20 $r7
;
||
    $dot $accumulate $r4 ($vu8) $r11 $r21
;
||
    + $r5 ($i64) $r3 $r4
;
||
    $st ($i64) [$r25 + 0] $r5
    $st ($i32) [$r31 + 0] $r29
;
||
    + $r25 ($i64) $r0 $r14
    ? ($i64) $r0 == $goto wc_10
;
we_12:
||
    + $r30 ($i64) $r26 -8
    + $r10 ($i64) $r26 -8
;
||
    $ld ($i32) $r1 [$r30 + 0]
;
||
    + $r6 ($i64) $r0 $r1
    + $r13 ($i64) $r1 1
;
||
    $st ($i32) [$r10 + 0] $r13
    ? ($i64) $r0 == $goto wc_7
;
we_9:
||
    $set $r2 0 8192
    + $r16 ($i64) $r0 3
;
||
    $ld ($i64) $r15 [$r28 + $r2]
    << $r16 ($i64) $r16 16
;
||
    $set $r2 0 10272
;
||
    | $r2 ($i64) $r2 $r16
;
||
    - $r16 ($i64) $r15 $r2
;
||
    ? ($i64) $r16 != $goto if_t_13
;
||
    ? ($i64) $r0 == $goto if_e_14
;
if_t_13:
||
    + $r2 ($i64) $r0 -1
;
||
    + $r1 ($i64) $r0 $r2
    ? ($i64) $r0 == $goto main_epilogue
;
if_e_14:
||
    + $r9 ($i64) $r0 504
    $set $r12 0 8192
;
||
    + $r12 ($i64) $r12 $r9
    $set $r9 0 2048
;
||
    + $r12 ($i64) $r28 $r12
;
||
    $ld ($i64) $r16 [$r12 + 0]
;
||
    + $r12 ($i64) $r0 3
;
||
    << $r12 ($i64) $r12 16
;
||
    | $r9 ($i64) $r9 $r12
;
||
    - $r12 ($i64) $r16 $r9
;
||
    ? ($i64) $r12 != $goto if_t_15
;
||
    ? ($i64) $r0 == $goto if_e_16
;
if_t_15:
||
    + $r9 ($i64) $r0 -2
;
||
    + $r1 ($i64) $r0 $r9
    ? ($i64) $r0 == $goto main_epilogue
;
if_e_16:
||
    $set $r17 0 32760
    $set $r18 0 8192
;
||
    + $r18 ($i64) $r18 $r17
    $set $r17 0 2048
;
||
    + $r18 ($i64) $r28 $r18
;
||
    $ld ($i64) $r12 [$r18 + 0]
;
||
    + $r18 ($i64) $r0 21
;
||
    << $r18 ($i64) $r18 16
;
||
    | $r17 ($i64) $r17 $r18
;
||
    - $r18 ($i64) $r12 $r17
;
||
    ? ($i64) $r18 != $goto if_t_17
;
||
    ? ($i64) $r0 == $goto if_e_18
;
if_t_17:
||
    + $r17 ($i64) $r0 -3
;
||
    + $r1 ($i64) $r0 $r17
    ? ($i64) $r0 == $goto main_epilogue
;
if_e_18:
||
    $set $r20 0 23200
    $set $r11 0 8192
;
||
    + $r11 ($i64) $r11 $r20
    $set $r20 0 6816
;
||
    + $r11 ($i64) $r28 $r11
;
||
    $ld ($i64) $r18 [$r11 + 0]
;
||
    + $r11 ($i64) $r0 11
;
||
    << $r11 ($i64) $r11 16
;
||
    | $r20 ($i64) $r20 $r11
;
||
    - $r11 ($i64) $r18 $r20
;
||
    ? ($i64) $r11 != $goto if_t_19
;
||
    ? ($i64) $r0 == $goto if_e_20
;
if_t_19:
||
    + $r20 ($i64) $r0 -4
;
||
    + $r1 ($i64) $r0 $r20
    ? ($i64) $r0 == $goto main_epilogue
;
if_e_20:
||
    + $r1 ($i64) $r0 1
    ? ($i64) $r0 == $goto main_epilogue
;
main_epilogue:
||
    + $r27 ($i64) $r0 $r26
;
||
    $ld ($i64) $r26 [$r27 + 0]
    $return
;
